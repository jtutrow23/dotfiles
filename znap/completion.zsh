# Make Homebrew + zsh-completions play nicely.
# If your repo already manages fpath/compinit globally, this can stay minimal.

# If you want to ensure Homebrew completions exist in fpath, uncomment:
# if command -v brew >/dev/null 2>&1; then
#   FPATH="$(brew --prefix)/share/zsh/site-functions:${FPATH}"
# fi
